# 验收清单（v2）

> 手动逐项验收。前提：双击 start.bat 启动，或后端已启动（8000）、前端已启动（5173）。

- [ ] 双击 start.bat 后浏览器自动打开应用（http://127.0.0.1:8000）

## 总站首页
- [ ] 登录后首页为「总站门户」：顶部今日概览（专注/次数/完成任务/连续天数）
- [ ] 「功能中心」卡片：高数复习（含掌握进度条）、番茄专注、任务管理、计划拆解、复习提醒、统计看板、AI 服务，点击卡片跳到对应页面
- [ ] 「预留功能区」显示 冲刺计划 / 雅思备考 / 迷你番茄钟 / 周月报告 四个占位卡片（不可点击，带「预留」标记）
- [ ] 深色 / 浅色主题下首页卡片与进度条显示正常

## 高数复习
- [ ] 顶栏「高数」进入复习页，目录显示 7 章，总进度 0/108
- [ ] 知识点列表正常渲染：普通文本与公式（KaTeX）混合显示，公式不依赖外网（断网也能显示）
- [ ] 勾选知识点「已掌握」，顶部总进度与对应章节进度实时更新
- [ ] 再次勾选可取消；「清除进度」有确认提示，确认后全部归零
- [ ] 搜索「极限」等关键字能过滤出对应知识点；清空关键词恢复全部
- [ ] 标签筛选（必考 / 必背 / 高频 / 证明题 / 定义题 / 应用题 / 易错）过滤正确，并显示各标签数量
- [ ] 章节 📝 笔记：填写保存后显示在章节标题下，刷新页面仍然存在
- [ ] 章节可折叠 / 展开，「折叠全部」「展开全部」生效
- [ ] 换一个账号登录，看不到上一个账号的掌握标记与笔记（数据隔离）
- [ ] 刷新页面后进度与笔记保持（写入数据库）

## 设置与数据备份
- [ ] 顶栏 ⚙️ 进入设置页，显示用户名与注册时间
- [ ] 点「导出备份」下载 JSON 文件，内容包含任务 / 计划 / 专注 / 打卡 / 复习 / 个性化设置 / 计划模板 / 高数进度与笔记，且不含 API Key
- [ ] 导入非 JSON 文件或非本应用备份文件时给出明确错误提示
- [ ] 导入备份前弹出确认框，明确提示会覆盖当前账号数据
- [ ] 导入后任务 / 计划 / 专注 / 复习 / 个性化设置 / 计划模板 / 高数进度恢复，计划父子关系、任务所属计划、专注关联任务均正确
- [ ] 导入到另一个账号不会影响原账号数据（隔离）
- [ ] 备份中 AI 配置恢复服务商 / 地址 / 模型，API Key 需重新填写
- [ ] 刷新页面后导入的数据保持（写入数据库）

## 云端端口（部署预留）
- [ ] 本地 `backend/run.py` 默认监听 127.0.0.1:8000
- [ ] 设置环境变量 `PORT=8123` 后 `python run.py` 监听 8123（云端注入端口生效）
- [ ] 设置 `HOST=0.0.0.0` 后可被局域网 / 外部访问
- [ ] 根目录存在 `Dockerfile` 与 `.dockerignore`，可构建容器镜像
- [ ] `render.yaml` 使用 `python run.py` 启动，`DATABASE_URL` 可指向 PostgreSQL

## 账号系统
- [ ] 注册页：填用户名/密码/邀请码可注册并自动登录（本地邀请码 studydash）
- [ ] 邀请码错误、用户名重复、密码过短时有明确提示
- [ ] 登录页：正确密码进入系统，错误密码提示「用户名或密码错误」
- [ ] 未登录访问首页/专注/任务/统计会被重定向到登录页
- [ ] 退出登录后回到登录页，刷新后仍保持退出
- [ ] 刷新页面后登录状态保持（token 存在 localStorage）
- [ ] 两个账号互不可见：A 建的任务、专注记录，B 看不到也改不了

## 个性化自定义系统
- [ ] 设置页出现「🎨 个性化」分区：外观 / 番茄钟时长 / 复习间隔 / 任务默认值 / 首页功能卡片 / 我的计划模板
- [ ] 外观：切「深色 / 浅色 / 跟随系统」后全站即时生效；5 色强调色（靛蓝/绿/玫红/琥珀/紫罗兰）切换后按钮与高亮色跟随变化
- [ ] 主题与强调色按账号保存：换一个账号登录显示该账号自己的外观，不串
- [ ] 番茄钟时长：增删时长（1–180 分钟、1–5 档）并设默认档后，专注页时长按钮与默认选中随之变化；只剩 1 档时不能删除
- [ ] 番茄时长非法值（超范围 / 默认值不在列表）保存时被拦截并提示
- [ ] 复习间隔：改间隔后新完成一个任务，复习页生成节点按新间隔；历史节点间隔不变
- [ ] 复习间隔保存后自动排序去重；非法值（超 1–365、超 8 项）被拦截
- [ ] 任务默认值：新建任务时科目下拉使用科目库、预计分钟默认值生效；习惯默认频率选「工作日 / 自定义星期」后新习惯任务带对应频率
- [ ] 习惯频率：工作日习惯在周末打卡返回错误提示；自定义星期未选中的星期同样被拦截；连续天数只累计应打卡日
- [ ] 首页功能卡片：隐藏「任务管理」后首页不再显示；上移 / 下移后顺序变化；刷新保持（写入数据库）
- [ ] 我的计划模板：新建模板（名称 + 子项）后，计划页「拆解」弹层出现「我的模板」并可选；模板可编辑 / 删除
- [ ] 计划拆解用自定义模板生成子计划数量与标题与模板一致
- [ ] 导出备份后导入：个性化设置与计划模板一并恢复
- [ ] 未登录访问页面外观回退为浏览器本地记忆（不影响登录后按账号生效）
- [ ] 两个账号的设置互相隔离：A 改的主题 / 时长 / 模板不影响 B

## 任务管理
- [ ] 在「任务」页添加任务：标题、科目、预计分钟，提交后出现在列表中
- [ ] 任务为空标题时被拦截（必填校验）
- [ ] 点击「开始做」状态变为进行中，再次点击变回待办
- [ ] 点击「完成」状态变为已完成，标题出现删除线
- [ ] 「重开」把已完成任务恢复为待办
- [ ] 删除任务前有确认弹窗，删除后列表更新
- [ ] 筛选 tabs（全部/待办/进行中/已完成）过滤正确

## 番茄钟
- [ ] 可切换 25/45/60 分钟
- [ ] 开始后倒计时走动，环形进度条随之变化
- [ ] 暂停后剩余时间保持不变，可继续
- [ ] 重置恢复为所选时长的完整时间
- [ ] 倒计时结束有提示音，弹出「记录本次专注」窗口
- [ ] 记录专注可关联任务或不关联，提交后弹窗关闭
- [ ] 计时期间离开页面再回来，倒计时状态保持（v1 不做后台计时，属已知限制）

## 统计
- [ ] 首页和统计页的「今日专注（分钟）/专注次数/完成任务」随操作更新
- [ ] 统计页「连续专注」卡片显示当前/最长天数与累计专注天数
- [ ] 近 105 天专注热力图渲染正确，颜色随专注分钟由浅到深，悬停显示日期与分钟数
- [ ] 近 7/14/30 天趋势图（ECharts 柱状图）渲染正确，切换天数即时刷新
- [ ] 刷新页面后数据仍然存在（SQLite 持久化）

## 习惯打卡
- [ ] 添加任务时勾选「设为习惯」，任务带 🔁 习惯标签，不显示「完成」按钮
- [ ] 习惯任务点「✅ 打卡」后变为已打卡，可撤销；重复打卡不报错
- [ ] 习惯列表显示 🔥 连续天数；连续两天打卡后 streak 为 2
- [ ] 撤销打卡后当日状态恢复未打卡，连续天数按实际记录计算
- [ ] 首页「今日打卡」卡片汇总今日已打卡 / 未打卡习惯
- [ ] 习惯数据按账号隔离：A 的习惯 B 看不到、不能代打卡

## 复习提醒
- [ ] 普通任务标记「完成」后，复习页「全部记录」出现 6 个节点（1/2/4/7/15/30 天）
- [ ] 任务重开再完成后不重复生成复习节点（防重复）
- [ ] 计划标记「完成」同样生成 6 个复习节点
- [ ] 把节点日期调到昨天后，该节点出现在「待复习」且标红「已逾期」
- [ ] 点「复习完成」后节点不再出现在待复习列表，显示完成时间
- [ ] 「一键完成到期」批量处理所有到期节点
- [ ] 复习数据按账号隔离：B 不能完成 A 的复习


## AI 服务接入
- [ ] 顶栏出现「AI」入口，未登录访问会跳登录页
- [ ] AI 设置页显示服务商卡片（OpenAI / DeepSeek / 通义千问 / 智谱 GLM / Kimi / 自定义），点击卡片自动填充接口地址与默认模型
- [ ] 填好 Base URL / 模型 / API Key 后点「测试连接」：成功显示模型回复与耗时，失败显示后端返回的具体错误
- [ ] 点「保存配置」后页面顶部出现「已配置 … Key sk-****」掩码提示；刷新页面后配置仍然保留
- [ ] API Key 输入框默认隐藏（password），点「显示」可查看明文；已保存后留空保存不改变原 Key
- [ ] 计划页某计划「拆解 → AI」：已配置时提示「将使用你的 AI 配置」，点开始能生成子计划
- [ ] 未配置时 AI 拆解提示「尚未配置 AI 服务」并提供「前往 AI 设置」链接
- [ ] 点「清除配置」后回到未配置状态，AI 拆解提示引导配置
- [ ] AI 配置按账号隔离：B 的 AI 设置页看不到 A 保存的 Key（只显示自己的或未配置）

## 后端
- [ ] 访问 http://127.0.0.1:8000/docs 能看到全部接口
- [ ] `pytest tests -v` 全部通过
- [ ] `backend\.venv\Scripts\python.exe scripts\selfcheck.py` 全部 PASS（依赖 / 数据库 / 上传目录 / 端口）
- [ ] 顶栏显示「后端在线」绿点；停掉后端后变「后端未启动」红点，点击可重检

## 学习文件（上传与运营整合）
- [ ] 「文件」页可上传 PDF：填分类 / 描述后上传成功，列表出现「待处理 / 待扫描」标签
- [ ] 上传 .exe 或把脚本改名 .pdf 上传会被拒绝并提示原因
- [ ] 超过大小限制（MAX_UPLOAD_MB）上传返回「超过大小限制」提示
- [ ] 上传后可下载，文件名（含中文）与内容一致
- [ ] 文件按账号隔离：A 上传的文件 B 列表看不到、下载 / 删除返回 404
- [ ] 未登录访问 /api/files 返回 401
- [ ] 普通用户访问管理员操作（放行 / 扫描）返回「需要管理员权限」
- [ ] 管理员（ADMIN_USERNAMES 中的账号）可「全部用户」视图看到所有文件，可放行 / 隔离 / 拒绝 / 重新扫描 / 标记整合
- [ ] 管理员隔离文件后：本人下载被拒（已隔离提示），管理员仍可下载；管理员放行后本人可重新下载
- [ ] 配置 SCAN_COMMAND 后上传：干净文件 scan_status=安全，病毒文件自动隔离（可用 fake_scan 脚本模拟）
- [ ] 上传目录结构正确：文件在 `backend/uploads/files/{user_id}/`（UUID 命名），隔离文件在 `backend/uploads/quarantine/`
- [ ] 顶栏「后端未启动」时给出 start.bat / 启动后端.bat 提示

## 计划拆解 + 流程图
- [ ] 「计划」页可创建根计划（标题必填、描述可选）
- [ ] 计划节点可「+子计划」逐层创建，层级不限
- [ ] 「拆解」弹层选模板（学习/项目/备考），点击后生成 4-6 个子计划
- [ ] 「拆解」弹层选 AI 未配置密钥时提示「未配置 LLM_API_KEY」（配置后按提示设置环境变量再试）
- [ ] 计划状态下拉可切换 待办/进行中/已完成
- [ ] 计划可编辑标题/描述；删除有确认弹窗，且级联删除全部子计划
- [ ] 计划节点「+任务」创建的任务出现在任务页，且带上所属计划标签
- [ ] 任务页「所属计划」下拉筛选正确，任务创建表单可指定所属计划
- [ ] 「流程图」标签页按计划层级绘制树状图，连接线与状态正确
- [ ] 流程图点击节点有选中高亮，点「在计划树中定位」会切回计划树并展开/滚动到该节点
- [ ] 「导出 SVG」「导出 PNG」分别下载可打开的图片文件
- [ ] 流程图工具栏可切换 纵向树 / 横向树 / 径向图，节点连线随布局正确变化
- [ ] 勾选「自由编辑」后可拖动节点到任意位置，导出后位置保持
- [ ] 工具栏可调全局节点宽高；点击节点后可在下方面板单独调整该节点宽高与填充/边框/文字颜色
- [ ] 五种主题（默认/蓝调/暖橙/深色/莫兰迪）一键切换，自定义颜色即时生效
- [ ] 「重置布局」清除所有手动位置/样式；切换布局后手动位置重置、颜色保留
- [ ] 刷新页面后计划/任务/流程图数据保持（SQLite 持久化）
- [ ] 两个账号的计划互相隔离：A 的计划 B 看不到、改不了、删不了
